{% extends 'base.html.twig' %}

{% block title %}Recipe{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-6">
            img
        </div>
        <div class="col-6">
            <h1>{{ recipe.name }}</h1>

            TODO category
        </div>
    </div>
{% endblock %}

{% block actions %}
    <a href="{{ path('app_recipe_edit', {'id': recipe.id}) }}" class="btn btn-secondary">edit</a>

    {{ include('recipe/_delete_form.html.twig') }}
{% endblock %}
